class Message
  def get_all_messages(user_id)
    #get all the user messages
  end

  def get_message(message_id)
    #get individual message by its message id
  end

  def delete_messages(user_id)
    #delete all of a given users messages
  end

  def delete_message(message_id)
    #delete individual message of user based on message id
  end

  def send_message(sender, recipient)
    #send message to recipient
  end

  def get_sent_messages(user_id)
    #get all sent user messages
  end

  def get_sent_message(message_id)
    #get individual message based on message id
  end

end
